pipeline {
    agent any
    triggers {
        githubPush()
    }
    stages {
        stage('file') {
            steps {
                sh '''
                    touch file-1
                    echo "hellow" > file-1
                    cat file-1
                '''
            }
        }
    }
}
